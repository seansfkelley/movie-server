cache results from IMDb, OMDbAPI (!!)

main should be renamed
main should be persistent
watch folders for changes (https://github.com/paulmillr/chokidar)

create simple web site that I can run through http-server
use bower for some simple hardcoded dependencies
pretty much just slam everything (data, libs) into the page so the client can deal with compiling it
